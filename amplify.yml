version: 1.1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm i moment
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
